org.gradle.jvmargs=-Xmx4G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Enable 16KB page size support
# The updated Stripe SDK (21.19.0) and dependencies are compatible with 16KB page sizes
# No additional configuration needed - compatibility is built into the SDK
